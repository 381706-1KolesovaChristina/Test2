let initialState = {
    friendsList: [
        { id: 1, name: "Dimych" },
        { id: 2, name: "Andrey" },
        { id: 3, name: "Sasha'" },
    ],
    friendsData: []
};

const sidebarsReduser = (state = initialState, action) => {

    return state;
}

export default sidebarsReduser;